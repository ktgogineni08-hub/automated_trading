# ==============================================================================
# TRADING SYSTEM ENVIRONMENT CONFIGURATION
# ==============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git (it's in .gitignore)
#
# To load these variables, use:
#   source .env (for bash/zsh)
#   OR use the provided setup script: source scripts/setup_env.sh
# ==============================================================================

# ------------------------------------------------------------------------------
# ZERODHA API CREDENTIALS (REQUIRED for Live Trading)
# ------------------------------------------------------------------------------
# Get your API credentials from: https://kite.zerodha.com/connect/login
# CRITICAL: These are the correct variable names (not KITE_*)

ZERODHA_API_KEY=your_api_key_here
ZERODHA_API_SECRET=your_api_secret_here

# SECURITY NOTES:
# 1. Generate new keys via Zerodha Console → API Settings
# 2. Never share these credentials or commit them to version control
# 3. Enable 2FA on your Zerodha account for additional security
# 4. Rotate credentials regularly if compromised

# ------------------------------------------------------------------------------
# DASHBOARD SECURITY (REQUIRED)
# ------------------------------------------------------------------------------
# Dashboard API key for authenticated access
# Generate a strong random key (min 32 characters recommended)

DASHBOARD_API_KEY=your_dashboard_api_key_here_min_32_chars

# Example: Generate with: openssl rand -hex 32
# Or use: python3 -c "import secrets; print(secrets.token_hex(32))"

# ------------------------------------------------------------------------------
# STATE ENCRYPTION (OPTIONAL - for encrypted state persistence)
# ------------------------------------------------------------------------------
# Master password for encrypting trading state files
# Leave commented out to disable state encryption

# TRADING_SECURITY_PASSWORD=your_secure_password_here

# ------------------------------------------------------------------------------
# DATA ENCRYPTION (OPTIONAL - for client data protection)
# ------------------------------------------------------------------------------
# Encryption key for sensitive client data (KYC, PII)
# Leave commented out to use ephemeral key (not persisted between runs)

# DATA_ENCRYPTION_KEY=your_base64_encoded_key_here

# Example: Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# ------------------------------------------------------------------------------
# TRADING MODE CONFIGURATION
# ------------------------------------------------------------------------------
# Trading mode: 'paper' (simulated) or 'live' (real money)
# Default: paper

TRADING_MODE=paper

# ------------------------------------------------------------------------------
# CLIENT IDENTIFICATION (OPTIONAL)
# ------------------------------------------------------------------------------
# Client ID for multi-client deployments
# Default: CLIENT_001

# TRADING_CLIENT_ID=CLIENT_001

# ------------------------------------------------------------------------------
# LOGGING CONFIGURATION (OPTIONAL)
# ------------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO

# LOG_LEVEL=INFO

# Log directory
# Default: logs/

# LOG_DIR=logs

# ------------------------------------------------------------------------------
# DEVELOPMENT/TESTING FLAGS (OPTIONAL)
# ------------------------------------------------------------------------------
# Force development mode (disables some production safeguards)
# WARNING: Only use for testing!
# Default: false

# FORCE_DEVELOPMENT_MODE=false

# Disable TLS verification for dashboard (NOT for production!)
# WARNING: Only use for local testing with self-signed certs
# Default: false

# DASHBOARD_DISABLE_TLS_VERIFY=false

# ------------------------------------------------------------------------------
# TOKEN STORAGE (OPTIONAL)
# ------------------------------------------------------------------------------
# Directory for storing Zerodha access tokens
# Default: ~/.config/trading-system

# ZERODHA_TOKEN_DIR=~/.config/trading-system

# Token file path
# Default: $ZERODHA_TOKEN_DIR/zerodha_token.json

# ZERODHA_TOKEN_FILE=$ZERODHA_TOKEN_DIR/zerodha_token.json

# Token encryption key
# Default: None (tokens stored in plaintext)

# ZERODHA_TOKEN_KEY=your_token_encryption_key_here

# ------------------------------------------------------------------------------
# DASHBOARD CONFIGURATION (OPTIONAL)
# ------------------------------------------------------------------------------
# Dashboard server URL
# Default: http://localhost:8080

# DASHBOARD_URL=http://localhost:8080

# Dashboard port
# Default: 8080

# DASHBOARD_PORT=8080

# ------------------------------------------------------------------------------
# PERFORMANCE MONITORING (OPTIONAL)
# ------------------------------------------------------------------------------
# Enable performance metrics collection
# Default: true

# ENABLE_PERFORMANCE_MONITORING=true

# ------------------------------------------------------------------------------
# TESTING ENVIRONMENT SETUP
# ------------------------------------------------------------------------------
# For running the full test suite, you need AT MINIMUM:
#   1. DASHBOARD_API_KEY (any strong random string)
#   2. ZERODHA_API_KEY (can be dummy for paper trading tests)
#   3. ZERODHA_API_SECRET (can be dummy for paper trading tests)
#
# Example minimal setup for testing:
#   export DASHBOARD_API_KEY="test_dashboard_key_$(openssl rand -hex 16)"
#   export ZERODHA_API_KEY="test_api_key_1234567890"
#   export ZERODHA_API_SECRET="test_api_secret_1234567890"
#   export TRADING_MODE="paper"
# ------------------------------------------------------------------------------

# ==============================================================================
# QUICK START
# ==============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual credentials
# 3. Load variables: source .env
# 4. Verify: echo $ZERODHA_API_KEY
# ==============================================================================
