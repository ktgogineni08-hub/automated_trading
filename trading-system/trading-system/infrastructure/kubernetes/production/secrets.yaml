apiVersion: v1
kind: Secret
metadata:
  name: trading-system-secrets
  namespace: trading-system-prod
type: Opaque
stringData:
  # These should be base64 encoded in production
  # Use kubectl create secret or external secret management
  ZERODHA_API_KEY: "REPLACE_WITH_ACTUAL_KEY"
  ZERODHA_API_SECRET: "REPLACE_WITH_ACTUAL_SECRET"
  DATABASE_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
